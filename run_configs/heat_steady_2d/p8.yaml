defaults:
  - _self_
  - override hydra/hydra_logging: none
  - override hydra/job_logging: none

# Physical parameters
Lx: 1.0          # Domain length in x
Ly: 1.0          # Domain length in y

# Boundary conditions
T_top: 0.759       # Fixed top boundary temperature
T_bottom: 0.384    # Fixed bottom boundary temperature
T_left: 0.409      # Fixed left boundary temperature
T_right: 0.713     # Fixed right boundary temperature

# Controllable parameters
dx: 0.01         # Grid spacing (determines resolution)
relax: 1.0       # SOR relaxation parameter
error_threshold: 1e-8  # Convergence threshold
T_init: 0.0      # Initial temperature field

# Simulator statics
record_dt: 10000
end_frame: 10

# Output configuration
dump_dir: "sim_res/heat_steady_2d/p8"  # Directory for output files
verbose: False   # Enable verbose output