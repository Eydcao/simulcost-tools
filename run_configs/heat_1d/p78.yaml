defaults:
  - _self_
  - override hydra/hydra_logging: none
  - override hydra/job_logging: none

# Static parameters
L: 0.169                # Wall thickness [m]
k: 0.52                # Thermal conductivity [W/m-K]
h: 3.24               # Convection coefficient [W/m²-K]
rho: 1799           # Density [kg/m³]
cp: 926            # Specific heat [J/kg-K]
T_inf: 5          # Ambient temperature [K]
T_init: 29         # Initial temperature [K]

# Tunable parameters
n_space: 1000          # Number of spatial points
cfl: 1.0              # CFL number for stability

# Simulator statics
record_dt: 70        # Time interval between recordings [s]
end_frame: 24        # Simulation end after certain number of frames

# Output configuration
dump_dir: "sim_res/heat_1d/p78"  # Directory for output files
verbose: False          # Enable verbose output